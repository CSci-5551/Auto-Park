/*
 * main.cpp
 *  -  The main program to run the parking function.
 */

#include "robot.h"
#define
      
int main(){
  
  // Initialize the Robot
  Robot parkbot = new Robot();
  parkbot.init();

  // Scan for parking space
  
    // Log scan data
  
  // When parking space is found, execute park function

    // Log parameters

    // Log status

  return 0;
}
